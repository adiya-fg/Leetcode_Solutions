class Solution {
public:
    vector<int> survivedRobotsHealths(vector<int>& positions, vector<int>& healths, 
        string directions) {
        vector<int> ans;

        int n = directions.size();
        for(int i=0; i<n; i++){
            if(directions[i]=='R'){
                st.push(make_pair(i))
            }
        }
        return ans;
    }
};